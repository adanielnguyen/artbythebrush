function updateDom(){
  const card = document.getElementById("artwork-like-panel-<%=@like.artwork.id%>");
  card.innerHTML = '<%= j render "shared/like_button", artwork:@like.artwork%>'
  // console.log(card);
}
updateDom();
