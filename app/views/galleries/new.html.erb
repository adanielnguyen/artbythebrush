  <div class="banner-container" height="980px">
    <div class="banner" >
      <h1>CURATE<br> <spam class="brush-word">GALLERY</spam></h1>
      </div>
      <div class="video">
          <video height="auto" width="100%" autoplay="autoplay" loop muted>
          <source src= <%= URI.encode("https://res.cloudinary.com/dybthmrxc/video/upload/v1592751084/ezgif-3-0bec3ed14ba1_iib8vs.mp4")%> type="video/mp4">
        Your browser does not support the video tag.
        </video>
    </div>
  </div>

<%= simple_form_for @gallery, class: "forum" do |f| %>
<div class="form-gallery-container">
  <div class="gallery-form garamond form-sticky">
    <section class="form-sticky form-margin">
    <%= f.input :name, label: "Gallery Name" %>
      <%= f.input :description %>
      <%= f.input :start_date %>
      <%= f.input :end_date %>
      <%= f.input :image, as: :file %>
      <div class="" id="highlight">
        <%= f.submit class: "btn btn-clear submit-photo-button" %>
      </div>
    </section>      
  </div>
  <div class="gallery-artwork w-65">
    <div class="card-grid-layout px-3">
      <% @artworks.each do |artwork| %>
        <input type="checkbox" class="hidden" value="<%= artwork.id %>" name="post[artwork_ids][]"  id="post_artwork_ids_<%= artwork.id %>">
        <div class="art" >
          <%= cl_image_tag(artwork.images.first.key)%>
          <div class="card-description">
            <p class=""><%= artwork.name %></p>
          </div>
        </div>
      <% end %>
        </div>
      </div>
    </div>

  </div>
</div>
<% end %>

