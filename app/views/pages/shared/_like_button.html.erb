            <% if current_user.likes.select { |f| f.artwork == artwork}.empty? %>
              <%= simple_form_for [artwork, @like] do |f| %>
                <%= f.button :submit  %>
              <% end %>
              <% else %>
             <%= link_to "Delete",
              like_path(current_user.likes.select { |f| f.artwork == artwork}.first),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
            <% end %>